#include <iostream>
//#include "hello/hello.h"
#include "lib/tmf.h"

using namespace std;

int main(int argc, char *argv[]) {
    std::string a = "hello";

    cout << a << endl << len(a);
    exit;
}
