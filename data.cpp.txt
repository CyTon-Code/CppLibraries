
/**
int main() {
    double x, a, c, b, y;

    c = input("c = ");
    a = input("a = ");
    x = input("x = ");

    b = pow(c, 3);
    a = (a * b) + c;
    x = pow(x * a, 2);
    y = sin(a / x) + log10(0.08) * exp(x);

    cout << "y= " << y << endl;
    exit;
}
 */
